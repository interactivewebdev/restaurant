import { colors } from "./colors";
import { fonts, fontSizes, fontWeight } from "./fonts";
import { space } from "./spacing";
import { sizes, lineHeights } from "./sizes";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeight,
};
